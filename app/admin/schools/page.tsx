import Link from "next/link";import {prisma} from "@/lib/prisma";import {requireAdmin} from "@/lib/authz";import {saveSchool} from "@/app/actions/admin-management";
export default async function Page(){const session=await requireAdmin();const schools=await prisma.school.findMany({where:{deletedAt:null},include:{settings:true,_count:{select:{students:true,users:true}}},orderBy:{name:"asc"}});return <main className="content"><div className="page-heading"><h1 className="brand">Schools</h1><Link className="secondary" href="/admin">Dashboard</Link></div>{session.user.role==="SUPER_ADMIN"&&<form action={saveSchool} className="panel form"><h2>Add School</h2><input className="input" name="name" placeholder="School name" required/><input className="input" name="code" placeholder="Code" required/><input className="input" name="address" placeholder="Address"/><input className="input" name="email" type="email" placeholder="Email"/><input className="input" name="phone" placeholder="Phone"/><input className="input" name="timezone" defaultValue="Australia/Brisbane"/><label><input type="checkbox" name="isActive" defaultChecked/> Active</label><button className="primary">Create School</button></form>}<section className="panel" style={{marginTop:18}}><div className="request-list">{schools.map(s=><div className="list-row" key={s.id}><div><strong>{s.name} · {s.code}</strong><div className="subtle compact">{s._count.students} students · {s._count.users} staff · {s.settings?.timezone}</div></div><div className="actions-row"><span className="badge">{s.isActive?"ACTIVE":"INACTIVE"}</span><Link className="secondary" href={`/admin/schools/${s.id}/settings`}>Settings</Link></div></div>)}</div></section></main>}
