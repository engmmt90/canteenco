import Link from "next/link";import {prisma} from "@/lib/prisma";import {requireAdmin} from "@/lib/authz";import {saveProduct} from "@/app/actions/admin-management";
export default async function Page(){await requireAdmin();const products=await prisma.product.findMany({where:{deletedAt:null},orderBy:[{sortOrder:"asc"},{name:"asc"}]});return <main className="content"><div className="page-heading"><h1 className="brand">Products</h1><Link className="secondary" href="/admin">Dashboard</Link></div><form action={saveProduct} className="panel form"><h2>Add Product</h2><input className="input" name="sku" placeholder="SKU" required/><input className="input" name="name" placeholder="Product name" required/><input className="input" name="category" placeholder="Category"/><input className="input" name="price" type="number" min="0" step=".01" placeholder="Price" required/><input className="input" name="sortOrder" type="number" defaultValue="0"/><textarea className="input" name="description" placeholder="Description"/><label><input type="checkbox" name="isActive" defaultChecked/> Active</label><button className="primary">Add Product</button></form><section className="panel" style={{marginTop:18}}><div className="request-list">{products.map(p=><div className="list-row" key={p.id}><div><strong>{p.name}</strong><div className="subtle compact">{p.sku} · {p.category||"Uncategorised"}</div></div><div><strong>${Number(p.price).toFixed(2)}</strong> <span className="badge">{p.isActive?"ACTIVE":"INACTIVE"}</span></div></div>)}</div></section></main>}
