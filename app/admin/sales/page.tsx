import Link from "next/link";import {prisma} from "@/lib/prisma";import {adminSchoolScope} from "@/lib/admin-scope";
export default async function Page({searchParams}:{searchParams:Promise<{q?:string;status?:string;school?:string}>}){const {session,schoolId:forced}=await adminSchoolScope();const p=await searchParams;const schoolId=forced||p.school||undefined;const q=(p.q||"").trim();const where:any={...(schoolId?{schoolId}:{}),...(p.status?{status:p.status}:{}),...(q?{OR:[{saleNumber:{contains:q,mode:"insensitive"}},{student:{OR:[{displayCode:{contains:q,mode:"insensitive"}},{firstName:{contains:q,mode:"insensitive"}},{lastName:{contains:q,mode:"insensitive"}}]}}]}:{})};const [rows,schools]=await Promise.all([prisma.sale.findMany({where,include:{student:true,school:true,cashier:true},orderBy:{createdAt:"desc"},take:200}),prisma.school.findMany({where:{isActive:true,deletedAt:null},orderBy:{name:"asc"}})]);return <main className="content"><div className="page-heading"><h1 className="brand">Sales</h1><Link className="secondary" href="/admin">Dashboard</Link></div><form className="panel actions-row">{session.user.role==="SUPER_ADMIN"?<select className="input" name="school" defaultValue={schoolId||""}><option value="">All schools</option>{schools.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select>:null}<input className="input" name="q" defaultValue={q} placeholder="Sale, student or 3C-001"/><select className="input" name="status" defaultValue={p.status||""}><option value="">All statuses</option><option>COMPLETED</option><option>REFUNDED</option><option>VOIDED</option></select><button className="primary">Search</button></form><section className="panel" style={{marginTop:18}}><div className="request-list">{rows.map(s=><Link className="list-row" href={`/admin/sales/${s.id}`} key={s.id}><div><strong>{s.saleNumber}</strong><div className="subtle compact">{s.student.firstName} {s.student.lastName} · {s.student.displayCode} · {s.school.name} · {s.cashier.fullName}</div></div><div><strong>${Number(s.total).toFixed(2)}</strong><div className="subtle compact">{s.status} · {s.createdAt.toLocaleString("en-AU")}</div></div></Link>)}</div></section></main>}
